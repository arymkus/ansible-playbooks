---
- hosts: 10.0.0.206
  tasks:
  - name: Install Telegraf
    apt:
      deb: https://dl.influxdata.com/telegraf/releases/telegraf_1.4.3-1_amd64.deb
  - name: Start Telegraf
    service:
      name: telegraf
      state: started
  - name: Start Telegraf
    service:
      name: telegraf
      state: stopped
  - name: configuring Telegraf
    lineinfile:
      path: /etc/telegraf/telegraf.conf
      regexp: '^urls = '
      line: 'urls = ["http://10.0.0.207:8086"]'
  - name: Start Telegraf
    service:
      name: telegraf
      state: started
